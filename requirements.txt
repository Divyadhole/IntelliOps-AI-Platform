# ---- core data & modelling -------------------------------------------------
pandas>=2.1
numpy>=1.26
scikit-learn>=1.4
scipy>=1.11
pyarrow>=15.0
PyYAML>=6.0
joblib>=1.3

# ---- gradient boosting -----------------------------------------------------
xgboost>=2.0
lightgbm>=4.1

# ---- explainability --------------------------------------------------------
shap>=0.44

# ---- warehouse -------------------------------------------------------------
SQLAlchemy>=2.0
psycopg2-binary>=2.9        # Postgres driver; sqlite needs nothing extra

# ---- experiment tracking ---------------------------------------------------
mlflow>=2.11

# ---- serving ---------------------------------------------------------------
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6
requests>=2.31

# ---- dashboard -------------------------------------------------------------
streamlit>=1.32

# ---- optional: dense embeddings and transformer sentiment ------------------
# The platform degrades to TF-IDF/LSA + a lexicon scorer without these, so they
# are optional by design. Install with:  pip install -r requirements-nlp.txt
